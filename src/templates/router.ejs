/*
 * Copyright © Bold Brand Commerce Sp. z o.o. All rights reserved.
 * See LICENSE for license details.
 */

<%= options.router.map(({ fullname }) =>`import ${'route_' + hash(fullname)} from '${fullname.replace(/\\/g,'/')}';`).join('\n') %>

export default {
<%= options.router.map(({ fullname }) =>`\troute_${hash(fullname)},`).join('\n') %>
};
