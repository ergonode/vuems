{
  "name": "vuems",
  "version": "0.12.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.8.0",
    "nuxt": "^2.14.7"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "push-dir": "^0.4.1"
  }
}
